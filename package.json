{
  "name": "@backkit/redis",
  "version": "0.2.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "exit 0",
    "postinstall": "node configure.js",
    "preversion": "test `git rev-parse --abbrev-ref HEAD` = \"main\" && exit 0 || exit 1",
    "postversion": "git push && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/backkit/redis.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/backkit/redis/issues"
  },
  "homepage": "https://github.com/backkit/redis#readme",
  "dependencies": {
    "@backkit/autoconf": "^0.1.1",
    "redis": "^4.6.5"
  },
  "publishConfig": {
    "access": "public"
  }
}
